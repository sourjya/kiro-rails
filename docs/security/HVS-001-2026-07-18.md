# Findings: kiro-rails — Hardcoded Value Scan

## Scan Manifest
- **Date:** 2026-07-18
- **Directories scanned:** `scripts/`, `.kiro/hooks/`, `install.sh`, `install.ps1`
- **Directories excluded:** `docs/`, `tests/fixtures/`, `node_modules/`, `.git/`
- **Files scanned:** ~25
- **Patterns executed:** C1 (UUIDs), C2 (URLs/ports), C3 (magic numbers), C4 (string enums), C5 (credentials), C6 (env assumptions)
- **Suppressions honored:** 0

---

### CRITICAL (must fix before release)

| # | File:Line | Category | Value | Remediation |
|---|-----------|----------|-------|-------------|
| — | — | — | — | None found ✅ |

---

### HIGH (fix in current sprint)

| # | File:Line | Category | Value | Remediation |
|---|-----------|----------|-------|-------------|
| — | — | — | — | None found ✅ |

---

### MEDIUM (backlog)

| # | File:Line | Category | Value | Remediation |
|---|-----------|----------|-------|-------------|
| 1 | `scripts/bug-scribe.sh:315,317,588,590` | C3 | `5` (context lines ±5) | Extract to `CONTEXT_LINES=5` constant at top of script |
| 2 | `scripts/hardcoded-value-scan.sh:54` | C3 | Port list `3000,5173,5432,...` inline in regex | Extract to `COMMON_PORTS="3000\|5173\|..."` constant |

---

### LOW (note and move on)

| # | File:Line | Category | Value | Remediation |
|---|-----------|----------|-------|-------------|
| 3 | `.kiro/hooks/*.kiro.hook` (12 files) | C3 | `"timeout": 10` / `15` / `30` | ACCEPTABLE — hook timeouts are config, not business logic |
| 4 | `scripts/bug-scribe.sh:355,440,622` | C4 | `OPEN`, `IN_PROGRESS` as raw strings in sed | ACCEPTABLE — these are template markers, not enum comparisons in business logic |
| 5 | `install.sh:16` | C2 | `https://raw.githubusercontent.com/...` | ACCEPTABLE — installer needs a hardcoded source URL (overridable via `KIRO_RAILS_BASE_URL`) |
| 6 | `scripts/smoke-test-install.sh:48-52` | C2 | `127.0.0.1:$PORT` | ACCEPTABLE — test harness spinning up a local server (the port is dynamic) |
| 7 | `scripts/export-to-claude.sh:332` | C2 | `https://json.schemastore.org/...` | ACCEPTABLE — JSON schema reference URL (standard, won't change) |

---

### ACCEPTABLE (documented, no action)

| # | File:Line | Category | Value | Justification |
|---|-----------|----------|-------|---------------|
| 3 | Hook timeouts | C3 | `10`/`15`/`30` | Hook timeout is infra config, not business logic. Changing it doesn't change behavior. |
| 4 | Status strings in bug-scribe | C4 | `OPEN`/`IN_PROGRESS` | These are sed replacement targets for the template, not enum comparisons in business logic. The template is the source of truth. |
| 5 | Installer URL | C2 | GitHub raw URL | Overridable via env var. Must have a default. |
| 6 | Smoke test localhost | C2 | `127.0.0.1` | Test harness — port is dynamically allocated. |
| 7 | JSON schema URL | C2 | `json.schemastore.org` | Immutable external schema reference. |

---

### SUPPRESSED (audit these periodically)

| # | File:Line | Category | Suppression Reason | Still Valid? |
|---|-----------|----------|--------------------|--------------|
| — | — | — | — | No suppressions in use |

---

## Verdict

**PASS — no CRITICAL or HIGH findings.** Two MEDIUM findings are cosmetic (named constants would be cleaner but the values are used in one place each and have comments explaining them).

## Feedback Loop: What the Hook Misses

Comparing this scan against what `scripts/hardcoded-value-scan.sh` would catch:

| Finding | Hook catches it? | Gap? |
|---------|-----------------|------|
| #1: `5` (context lines) | ❌ No — hook doesn't scan for single-digit magic numbers (too many false positives) | **Not a gap** — this is correct. Single digits in bash scripts are ubiquitous. |
| #2: Port list in the hook itself | ❌ No — hook skips files in `scripts/` | **Not a gap** — correct exclusion. The hook IS the constants for this concern. |
| #3-7: ACCEPTABLE | ❌ Hook skips config/test/script files | **Correct** — these are intentional exclusions. |

**Conclusion: The hook's exclusion patterns are correctly tuned.** No regex additions needed. The scan confirms the hook catches what it should and correctly ignores what it should ignore.
